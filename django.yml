image: 3.10-alpine3.16

before_script:
  - pip install tox==2.9.1

cache:
  paths:
  - .tox

pep8:
  stage: check_style
  script:
   - tox -v -e pep8

docstyle:
  stage: check_style
  script:
   - tox -v -e docstyle